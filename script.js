let token="",bot=null;const $=id=>document.getElementById(id);async function api(method,body={}){const r=await fetch(`https://api.telegram.org/bot${token}/${method}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});return r.json()}function log(x){$("log").textContent+="\n> "+x}
$("connectBtn").onclick=async()=>{token=$("token").value.trim();$("error").textContent="";if(!token)return $("error").textContent="أدخل الـToken";try{const d=await api("getMe");if(!d.ok)throw Error(d.description);bot=d.result;$("connect").classList.add("hidden");$("app").classList.remove("hidden");$("status").textContent="● ONLINE";$("status").style.color="#3fe09d";$("botName").textContent=bot.first_name;$("botUser").textContent="@"+(bot.username||"");$("dashName").textContent=bot.first_name;$("botInfo").innerHTML=`<div>Name: ${bot.first_name}</div><div>Username: @${bot.username||"-"}</div><div>ID: ${bot.id}</div>`;log("CONNECTED: @"+(bot.username||""))}catch(e){$("error").textContent="فشل الاتصال: "+e.message}};
$("disconnect").onclick=()=>{token="";bot=null;$("app").classList.add("hidden");$("connect").classList.remove("hidden");$("token").value="";$("status").textContent="● OFFLINE";$("status").style.color=""};
document.querySelectorAll("nav button").forEach(b=>b.onclick=()=>{document.querySelectorAll("nav button").forEach(x=>x.classList.remove("active"));b.classList.add("active");document.querySelectorAll(".page").forEach(x=>x.classList.add("hidden"));$(b.dataset.page).classList.remove("hidden")});
$("sendMessage").onclick=async()=>{let id=$("chatId").value.trim(),text=$("message").value;if(!id||!text)return alert("أدخل Chat ID والرسالة");let d=await api("sendMessage",{chat_id:id,text});log(d.ok?"MESSAGE SENT":"ERROR: "+d.description);alert(d.ok?"تم الإرسال":"فشل الإرسال")};
$("sendBroadcast").onclick=async()=>{let ids=$("ids").value.split(/\n|,/).map(x=>x.trim()).filter(Boolean),text=$("broadcastMsg").value;if(!ids.length||!text)return alert("أدخل IDs والرسالة");let ok=0;for(const id of ids){let d=await api("sendMessage",{chat_id:id,text});if(d.ok)ok++}log(`BROADCAST: ${ok}/${ids.length}`);alert(`تم الإرسال إلى ${ok} من ${ids.length}`)};